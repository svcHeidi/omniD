# omnidriver-cardiac

The cardiacFoam electrophysiology domain plugin for `omnidriver`. Validates
electrophysiology models, checks Purkinje network definitions, and ensures
PVJ coupling correctness. Depends on both `omnidriver` and
`omnidriver-openfoam`.
