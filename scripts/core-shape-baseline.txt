# Recorded OpenFOAM-layout debt in core (spec 2026-09-25 §6). Format:
# <path relative to core src>\t<token>\t<count>\t<reason>
# This is debt, not a waiver: scripts/check-core-shape.py fails on any new
# (file, token) pair, any higher count, and any count that shrank without
# this file being edited to match -- so the total can only go down.
cli.py	bashrc	26	The `--environment-bashrc`/`environment_bashrc` CLI flag and the `explicit_bashrc` parameter it threads through `_context_from_entry` into `strict_plan` (and its plan/step/run call sites). Names the concept generically ("environment-sourcing script (e.g. its bashrc)") but the parameter and flag spelling itself predate the supplied-vs-discovered split (future/ENVIRONMENT_CONTRACT.md §12); not yet renamed off the OpenFOAM-specific shell-profile word.
core/compatibility.py	bashrc	4	`explicit_bashrc` keyword-only parameter on the three `legacy_*` shims (`legacy_environment_diagnostics`, `legacy_load_environment`) that stand in for a plugin predating `EnvironmentPreflightCapability`. The shims immediately `del explicit_bashrc` -- the name only exists to match the capability's signature, which itself carries the debt below.
core/plugin_capabilities.py	bashrc	11	`EnvironmentPreflightCapability.diagnostics`/`.load`'s `explicit_bashrc` parameter and its docstring, which documents the Tier 3 rename from `openfoam_bashrc` (future/ENVIRONMENT_CONTRACT.md §10): the protocol generalized to any environment-sourcing script but kept the OpenFOAM-shaped word `bashrc` in the parameter name and CLI flag rather than something solver-neutral like `environment_script`.
core/plugin_interface.py	bashrc	2	`PluginAdapter.get_environment_diagnostics`/`get_loaded_environment`'s `explicit_bashrc` parameter, mirroring `EnvironmentPreflightCapability` (core/plugin_capabilities.py) so an adapter's override signature matches the protocol it implements; same unresolved rename.
core/runtime/generic_case.py	bashrc	14	`_ExecutionContext`-adjacent execution path's `explicit_bashrc` parameter (see `get_loaded_environment` call sites) threaded from the CLI's `explicit_bashrc` through case staging/execution helpers so the environment-sourcing script reaches `get_loaded_environment` at run time; same rename debt as cli.py.
core/runtime/generic_case.py	case.foam	9	The `touch_case_foam` parameter (threaded through `_normalize_case_specs`/its callers) and the matching `"touch_case_foam"` key read from and written into `CaseConfig.params` (both the direct-payload branch and the per-item `item.get("touch_case_foam", ...)` merge). Found only once matching was normalised to catch snake_case spellings of `case.foam` (review 2026-09-25 C-I1); it is the same "touch the ParaView reader marker file" concept as the `case.foam` literal, just spelled as a Python identifier and a dict key instead of a dotted string.
core/runtime/provenance_inputs.py	processor	3	The `processor_dir` loop variable in the required-inputs walk, iterating `case_root.glob(f"{decomposition_prefix}*")` where `decomposition_prefix` is itself plugin-supplied (`decomposition_dirname_prefix(driver_context)`, generic). Only the local variable's *name* assumes OpenFOAM's `processorN` convention; the value it walks is adapter-declared and could be any prefix.
core/runtime/run_document_exec.py	FOAM_	1	Known, accepted substring hit, not coupling: `LEGACY_ALLOWED_RUNS_ROOT_ENV = "DRIVERFOAM_ALLOWED_RUNS_ROOT"` contains "FOAM_" only because it is the legacy environment-variable name from this project's own former name ("DriverFoam"), read alongside the current `OMNIDRIVER_ALLOWED_RUNS_ROOT` so an operator's existing setting for the allowed-runs-root safety boundary is not silently dropped. `FOAM_` is intentionally matched as an unbounded substring rather than a boundaried token (review 2026-09-25: a boundary would stop catching real `OPENFOAM_*` coupling elsewhere), so this hit on the project's own former name `DRIVERFOAM_` is an accepted, not a special-cased, consequence of that choice -- not OpenFOAM-the-solver.
core/strict_planning.py	bashrc	12	`explicit_bashrc` parameter on `strict_plan` and the workflow-dag environment-loading helpers it calls (`get_loaded_environment` via `EnvironmentPreflightCapability`), and the CLI's own `explicit_bashrc` argument passed straight through; same environment-loading seam and rename debt as cli.py and core/plugin_capabilities.py.
